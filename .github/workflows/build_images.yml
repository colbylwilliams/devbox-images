name: Build Images

concurrency: ${{ github.ref }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'images/**'
      - 'scripts/**'
      - 'tools/**'
      - '.github/**'

jobs:
  prepare:
    runs-on: windows-latest

    env:
      TOOLS_PATH: ${{ format('{0}{1}tools{1}', github.workspace, (runner.os == 'Windows' && '\' || '/') ) }}

    outputs:
      build: ${{ steps.images.outputs.build }}
      images: ${{ steps.images.outputs.images }}

    steps:
      - uses: actions/checkout@v2

      - name: Get Image Definitions
        id: images
        shell: python
        run: ${{ env.TOOLS_PATH }}images.py

  build:
    needs: prepare
    runs-on: windows-latest

    # this is needed to avoid workflow errors in case of an empty matrix
    if: ${{ needs.prepare.outputs.build == 'true' }}

    env:
      TOOLS_PATH: ${{ format('{0}{1}tools{1}', github.workspace, (runner.os == 'Windows' && '\' || '/') ) }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.images) }}

    steps:
      - uses: actions/checkout@v2

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build Image
        shell: python
        run: ${{ env.TOOLS_PATH }}build.py --suffix ${{ github.run_number }} --images ${{ matrix.name }} --packer
